# to find the maximum frequency occuring of ones in an given array
def count_maxones():
int counter=0,int max=0
for i inn range( :arr.size()):
  if arr[i]==1:
    counter++
    max= max(max,count)
  else:
    counter = 0
return max
