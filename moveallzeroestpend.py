def move_zeroes(arr):
    index=0
    for i in range (len[arr]):
        if arr[i]!=0:
            arr[inde]=arr[i]
            index+=1
            while index<len(arr)
            arr[index]=0
            index =+1
            
    return arr
